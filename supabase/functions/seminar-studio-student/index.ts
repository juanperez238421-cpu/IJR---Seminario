import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const allowedOrigins=new Set(["https://juanperez238421-cpu.github.io","http://localhost:8000","http://127.0.0.1:8000"]);
const TRACKS:Record<string,string>={"Web Development":"web","Python & Data Science":"data-science","Defensive Cybersecurity":"cybersecurity","3D Design + Programming":"3d-programming","Robotics & Automation":"robotics"};
const TOPICS=new Set(Object.keys(TRACKS)),GROUPS=new Set(["11-A","11-B","11-C"]),MODES=new Set(["Individual","Pareja"]);
function cors(origin:string|null){return{"Access-Control-Allow-Origin":origin&&allowedOrigins.has(origin)?origin:"null","Access-Control-Allow-Headers":"content-type, apikey, authorization, x-client-info","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store","Vary":"Origin"}}
function json(origin:string|null,status:number,body:unknown){return new Response(JSON.stringify(body),{status,headers:cors(origin)})}
function text(v:unknown,max:number){return typeof v==="string"?v.trim().slice(0,max):""}function nullableText(v:unknown,max:number){const x=text(v,max);return x||null}
async function sha256(value:string){const digest=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value));return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,"0")).join("")}
function validToken(t:string){return/^[a-f0-9]{48,128}$/i.test(t)}
function publicProfile(r:Record<string,unknown>){return{response_id:r.response_id,full_name:r.full_name,group_code:r.group_code,topics:r.topics,first_choice:r.first_choice,track_slug:r.track_slug,work_mode:r.work_mode,partner_name:r.partner_name,project_idea:r.project_idea,project_title:r.project_title,repo_full_name:r.repo_full_name,uml_url:r.uml_url,sprint_current:r.sprint_current,progress_percent:r.progress_percent,next_goal:r.next_goal,status:r.status,updated_at:r.updated_at}}
Deno.serve(async(req:Request)=>{const origin=req.headers.get("Origin");if(req.method==="OPTIONS"){if(!origin||!allowedOrigins.has(origin))return json(origin,403,{error:"origin_denied"});return new Response(null,{status:204,headers:cors(origin)})}if(req.method!=="POST")return json(origin,405,{error:"method_not_allowed"});if(!origin||!allowedOrigins.has(origin))return json(origin,404,{error:"not_found"});const url=Deno.env.get("SUPABASE_URL")??"",service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";if(!url||!service)return json(origin,503,{error:"backend_unavailable"});const admin=createClient(url,service,{auth:{persistSession:false,autoRefreshToken:false}});try{const body=await req.json(),action=text(body?.action,32),editToken=text(body?.edit_token,160);if(!validToken(editToken))return json(origin,400,{error:"invalid_edit_token"});const tokenHash=await sha256(editToken),ipHash=await sha256((req.headers.get("x-forwarded-for")??"").split(",")[0].trim()),userAgent=(req.headers.get("user-agent")??"").slice(0,1000);if(action==="register"){const fullName=text(body?.full_name,140),groupCode=text(body?.group_code,8),topics=Array.isArray(body?.topics)?body.topics.map((x:unknown)=>text(x,80)).filter((x:string)=>TOPICS.has(x)):[],firstChoice=text(body?.first_choice,80),workMode=text(body?.work_mode,16),partnerName=nullableText(body?.partner_name,140),projectIdea=nullableText(body?.project_idea,1200);if(fullName.length<3||!GROUPS.has(groupCode)||topics.length<1||topics.length>5||!TOPICS.has(firstChoice)||!topics.includes(firstChoice)||!MODES.has(workMode))return json(origin,400,{error:"invalid_registration"});if((workMode==="Pareja"&&(!partnerName||partnerName.length<3))||(workMode==="Individual"&&partnerName))return json(origin,400,{error:"invalid_partner"});const trackSlug=TRACKS[firstChoice];const{data:existing}=await admin.from("seminar_studio_profiles").select("id,response_id").eq("edit_token_hash",tokenHash).maybeSingle();let row;if(existing){const result=await admin.from("seminar_studio_profiles").update({full_name:fullName,group_code:groupCode,topics,first_choice:firstChoice,track_slug:trackSlug,work_mode:workMode,partner_name:workMode==="Pareja"?partnerName:null,project_idea:projectIdea,updated_at:new Date().toISOString(),last_student_activity_at:new Date().toISOString()}).eq("id",existing.id).select().single();if(result.error)throw result.error;row=result.data}else{const result=await admin.from("seminar_studio_profiles").insert({edit_token_hash:tokenHash,full_name:fullName,group_code:groupCode,topics,first_choice:firstChoice,track_slug:trackSlug,work_mode:workMode,partner_name:workMode==="Pareja"?partnerName:null,project_idea:projectIdea}).select().single();if(result.error)throw result.error;row=result.data}await admin.from("seminar_studio_events").insert({profile_id:row.id,event_type:"register",payload:{track_slug:trackSlug,group_code:groupCode},ip_hash:ipHash,user_agent:userAgent});return json(origin,200,{ok:true,profile:publicProfile(row)})}const{data:profile,error:profileError}=await admin.from("seminar_studio_profiles").select("*").eq("edit_token_hash",tokenHash).maybeSingle();if(profileError)throw profileError;if(!profile)return json(origin,404,{error:"profile_not_found"});if(action==="load"){await admin.from("seminar_studio_events").insert({profile_id:profile.id,event_type:"load",payload:{},ip_hash:ipHash,user_agent:userAgent});return json(origin,200,{ok:true,profile:publicProfile(profile)})}if(action==="update"){const patch:Record<string,unknown>={updated_at:new Date().toISOString(),last_student_activity_at:new Date().toISOString()};if(body?.repo_full_name!==undefined){const repo=nullableText(body.repo_full_name,180);if(repo&&!/^[A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+$/.test(repo))return json(origin,400,{error:"invalid_repo"});patch.repo_full_name=repo}if(body?.uml_url!==undefined){const uml=nullableText(body.uml_url,1000);if(uml&&!/^https:\/\//i.test(uml))return json(origin,400,{error:"invalid_uml_url"});patch.uml_url=uml}if(body?.project_title!==undefined)patch.project_title=nullableText(body.project_title,180);if(body?.next_goal!==undefined)patch.next_goal=nullableText(body.next_goal,500);if(body?.sprint_current!==undefined){const sprint=Number(body.sprint_current);if(!Number.isInteger(sprint)||sprint<1||sprint>8)return json(origin,400,{error:"invalid_sprint"});patch.sprint_current=sprint}if(body?.progress_percent!==undefined){const progress=Number(body.progress_percent);if(!Number.isInteger(progress)||progress<0||progress>100)return json(origin,400,{error:"invalid_progress"});patch.progress_percent=progress}const result=await admin.from("seminar_studio_profiles").update(patch).eq("id",profile.id).select().single();if(result.error)throw result.error;await admin.from("seminar_studio_events").insert({profile_id:profile.id,event_type:"student_update",payload:{sprint_current:result.data.sprint_current,progress_percent:result.data.progress_percent},ip_hash:ipHash,user_agent:userAgent});return json(origin,200,{ok:true,profile:publicProfile(result.data)})}return json(origin,404,{error:"not_found"})}catch(error){console.error(error);return json(origin,400,{error:"invalid_request"})}});
